/**
 * Ternary operator
 */

const age = 24;


// <boolean> ? <value1> : <value2>
/**
 * Om <boolean ar sann, da far vi value1
 * Annars far vi value2.
 */

//                 <boolean>   ? <value1>  : <value2>
const firstname = age === 24 ? "Sebastian" : "John";

// ? = if
// : = else

console.log(firstname);